province_event = {
	id = 2202

	major = no

	trigger = {
		owner = {primary_culture = necromantic_culture}
		culture = necromantic_culture
		NOT = {has_province_flag = necro_connected}
		OR = {
			is_capital = true
			any_neighbor_province = {
				has_province_flag = necro_connected
			}
		}
	}
	
	mean_time_to_happen = {
		days = 1
	}

	hidden = yes

	immediate = {
		hidden_effect = {
			set_province_flag = necro_connected
		}
	}
}
